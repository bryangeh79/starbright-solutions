# Next Action

## 唯一优先动作
- 文件：`src/pages/faq.js`
- 目标：再次确认该页在当前磁盘版本下可稳定编译、可稳定响应，不再拖累 `/` 和 `/contact`
- 原因：`faq.js` 曾是 Phase 5 的主要阻塞点，也是本地验收链路反复掉回 ERROR / 500 的核心风险
- 验收方式：写入后先用 `exec` 读取文件前后内容确认纯净，再重新触发本地请求，只验证 `/` 与 `/faq` 的连续稳定响应
